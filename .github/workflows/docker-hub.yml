# name: Docker Hub Build and Push

# on:
#   workflow_dispatch:  # Manual trigger
#   push:
#     tags:
#       - 'v*'  # Trigger on version tags
#     branches:
#       - capstone

# env:
#   DOCKER_HUB_REPOSITORY: murewaashiru/scalagos-capstone  # Replace with your Docker Hub username

# jobs:
#   build-and-push-dockerhub:
#     runs-on: ubuntu-latest
    
#     steps:
#     - name: Checkout repository
#       uses: actions/checkout@v4

#     - name: Set up Docker Buildx
#       uses: docker/setup-buildx-action@v3

#     - name: Log in to Docker Hub
#       uses: docker/login-action@v3
#       with:
#         username: ${{ secrets.DOCKER_HUB_USERNAME }}
#         password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

#     - name: Extract metadata for Docker
#       id: meta
#       uses: docker/metadata-action@v5
#       with:
#         images: ${{ env.DOCKER_HUB_REPOSITORY }}
#         tags: |
#           type=ref,event=branch
#           type=ref,event=tag
#           type=sha,prefix={{branch}}-
#           type=raw,value=latest,enable={{is_default_branch}}

#     - name: Build and push to Docker Hub
#       uses: docker/build-push-action@v5
#       with:
#         context: .
#         platforms: linux/amd64,linux/arm64
#         push: true
#         tags: ${{ steps.meta.outputs.tags }}
#         labels: ${{ steps.meta.outputs.labels }}
#         cache-from: type=gha
#         cache-to: type=gha,mode=max

#     - name: Generate Docker Hub summary
#       run: |
#         echo "## 🐳 Docker Hub Deployment" >> $GITHUB_STEP_SUMMARY
#         echo "- **Repository**: \`${{ env.DOCKER_HUB_REPOSITORY }}\`" >> $GITHUB_STEP_SUMMARY
#         echo "- **Tags**: ${{ steps.meta.outputs.tags }}" >> $GITHUB_STEP_SUMMARY
#         echo "- **Platforms**: linux/amd64, linux/arm64" >> $GITHUB_STEP_SUMMARY
#         echo "- **Pull Command**: \`docker pull ${{ env.DOCKER_HUB_REPOSITORY }}:latest\`" >> $GITHUB_STEP_SUMMARY
